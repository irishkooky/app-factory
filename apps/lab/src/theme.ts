import { createTheme } from '@mantine/core'

export const theme = createTheme({
  primaryColor: 'indigo',
  defaultRadius: 'md',
  fontFamily: "'Inter', 'Hiragino Sans', sans-serif",
  fontFamilyMonospace: "'DM Mono', 'SFMono-Regular', 'Menlo', monospace",
  headings: { fontWeight: '700' },
})
